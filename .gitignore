# --- Swift Package Manager ---
.build/
.swiftpm/
# Package.resolved is intentionally NOT ignored: this is an executable app,
# so a committed lockfile gives reproducible builds. (None exists yet because
# the project has no external dependencies — it will appear if any are added.)

# --- macOS app build outputs (regenerated by scripts/build-app.sh) ---
UTCMenuBar.app/
*.dSYM/
DerivedData/

# --- macOS filesystem junk ---
.DS_Store
._*
.Spotlight-V100
.Trashes

# --- Editors ---
*.swp
*~
.idea/

# --- Claude Code (workflow private) ---
.claude/

# --- Release artifacts (uploaded to GitHub Releases instead of committed) ---
UTCMenuBar-*.zip
